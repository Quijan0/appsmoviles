import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.edu.ue.lotery',
  appName: 'LoteryUE',
  webDir: 'dist/loteria/browser'
};

export default config;
